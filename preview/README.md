# PortKill Remotion Preview

This Remotion project renders the animated README preview at:

```text
../assets/portkill-preview.gif
```

## Commands

```bash
npm install
npm run dev
npm run render:gif
```

The composition id is `PortKillPreview`.
